% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_cardio.R
\name{predict_cardio}
\alias{predict_cardio}
\title{Predict Cardiovascular Disease}
\usage{
predict_cardio(new_data, model_type = c("xgboost", "glm", "rf"))
}
\arguments{
\item{new_data}{A data.frame. Must contain the feature columns used during training}

\item{model_type}{A character string specifying the model to use. Options:
\itemize{
  \item "xgboost" (default) - Extreme Gradient Boosting
  \item "glm" - Logistic Regression
  \item "rf" - Random Forest
}}
}
\value{
An integer vector: 0 indicates no disease, 1 indicates disease.
}
\description{
Takes a data frame containing patient features and a selected model type as input,
and returns a binary prediction outcome (0 or 1).
}
\examples{
# Example Data
patient <- data.frame(
  age = 50, gender = factor(1), height = 165, weight = 70,
  ap_hi = 120, ap_lo = 80, cholesterol = factor(1), gluc = factor(1),
  smoke = 0, alco = 0, active = 1
)

# Predict using XGBoost
predict_cardio(patient, model_type = "xgboost")

# Predict using GLM
predict_cardio(patient, model_type = "glm")
}
